/**
 * @flow
 */

'use strict';

export type UIState = 'home' | 'game' | 'completed';

const UIStates = {
  HOME: 'home',
  GAME: 'game',
  COMPLETED: 'completed',
  SIMULATION: 'simulation',
};

export default UIStates;
