/**
 * @flow
 */

'use strict';

export type Character = 'human' | 'random' | 'greedy' | 'smart-greedy';

const Characters = {
  HUMAN: 'human',
  RANDOM: 'random',
  GREEDY: 'greedy',
  SMART_GREEDY: 'smart-greedy',
};

export default Characters;
